import { $WIN } from '../_constants';

export default function() {

	// const lang = $('.js-lang');

	// const checkLang = () => {

	// 	lang.each(function() {

	// 		const $this = $(this);
	// 		const $track = $this.find('.js-lang-track');
	// 		const $items = $this.find('.js-lang-item');

	// 		const itemsWidth = [].reduce.call($items, (current, next) => {
	// 			return current + $(next).outerWidth();
	// 		}, 0);

	// 		$track.css()

	// 		console.log(itemsWidth);

	// 	});

	// };

	// let resizeDebounce = null;
	// $WIN.on('resize', (e) => {
	// 	clearTimeout(resizeDebounce);
	// 	resizeDebounce = setTimeout(checkLang, 100);
	// });

};